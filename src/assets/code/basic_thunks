// Eager version
function yell (text) {
  console.log(text + '!')
}

yell('Salut') // 'Salut!'

// Lazy (or "thunked") version
function thunkedYell (text) {
  return function thunk () {
    console.log(text + '!')
  }
}

const thunk = thunkedYell('Salut') // no action yet.

// wait for it…

thunk() // 'Salut!'

const yell        = text =>       console.log(text + '!')
const thunkedYell = text => () => console.log(text + '!')
