import React from 'react';
import { Text } from 'spectacle';

const handleClass = {
  position: 'absolute',
  top: '100%',
  left: '90%',
  color: '#ffb732',
};

export default () => (
  <Text style={handleClass}>@_arturdas</Text>
);

